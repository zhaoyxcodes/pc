<view class="backview" wx:if="{{islogin}}">
<view class="authview"  wx:if="{{islogin}}">
<button  style="height:100%"open-type="getUserInfo" bindgetuserinfo="bindGetUserInfo">微信用户快速登录</button>
</view>
</view>

